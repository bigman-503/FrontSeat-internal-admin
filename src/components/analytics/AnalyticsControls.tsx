import React from 'react';

interface AnalyticsControlsProps {
  loading: boolean;
}

export function AnalyticsControls({
  loading
}: AnalyticsControlsProps) {
  return (
    <div className="space-y-4">
      {/* No controls - completely clean interface */}
    </div>
  );
}
